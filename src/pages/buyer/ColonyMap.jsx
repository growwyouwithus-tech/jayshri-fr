import { Box, Typography } from '@mui/material'

const ColonyMap = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold" mb={4}>
        Explore Colonies
      </Typography>
      <Typography>Interactive colony map will be displayed here</Typography>
    </Box>
  )
}

export default ColonyMap
